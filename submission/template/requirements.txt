# 平台评测环境是 CUDA 13.0 / driver 580，A6000 sm_86。
# 详见 ../ENV_NOTES.md。
#
# 推荐：先在本目录用 pipreqs 生成基础列表，再人工写死版本号。
#   pip install pipreqs
#   pipreqs ./ --encoding=utf8 --force --mode no-pin
#   # 然后人工编辑加 ==X.Y.Z
#
# ⛔ 不要 pip freeze（会带 jupyter / wandb / tensorboard 等无关库 → 评测装包失败）
#
# 推荐组合（CUDA 13 原生）：
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.10.0
numpy==2.4.3
pandas==3.0.1
